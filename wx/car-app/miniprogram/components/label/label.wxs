var formatlabel = function(item,length){
  if(length < 3){
     return item.slice(0,10);

  }else{
      return item.slice(0,6)
  }
}
var isShow = function(index){
  if(index >2){
    return false
  }else{
    return true
  }
}
module.exports={
  formatlabel:formatlabel,
  isShow:isShow
};